import React from 'react';
import SearchBar from './SearchBar';
import VisibleChain from './VisibleChain';

const App = () => (
    <div>
        <SearchBar />
        <VisibleChain />
    </div>
);

export default App;
